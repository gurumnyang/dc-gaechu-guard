<%- include('partials/header') %>

<div class="container-fluid mt-4">
  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-header bg-danger text-white">
          <i class="fas fa-exclamation-triangle me-2"></i>오류가 발생했습니다
        </div>
        <div class="card-body">
          <h5 class="card-title">요청 처리 중 문제가 발생했습니다</h5>
          <p class="card-text">
            <% if (typeof error !== 'undefined') { %>
              <%= error %>
            <% } else { %>
              알 수 없는 오류가 발생했습니다. 관리자에게 문의하세요.
            <% } %>
          </p>
          <a href="javascript:history.back()" class="btn btn-primary">
            <i class="fas fa-arrow-left me-2"></i>이전 페이지로 돌아가기
          </a>
          <a href="/dashboard" class="btn btn-secondary">
            <i class="fas fa-home me-2"></i>대시보드로 이동
          </a>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
